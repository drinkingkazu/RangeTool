(*) Ask Wes's opinion about changing insert/emplace function name w/ lower case as these override the original implementation (otherwise there's no check of overlapping regions

(*) override insert_hint 

